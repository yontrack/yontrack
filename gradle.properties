# Memory settings for the build
org.gradle.jvmargs=-Xmx6144m

# Task outputs are reused across builds (#1870). On CI the local cache lives in the Gradle User
# Home, which setup-gradle saves and restores, so a module a commit does not touch is neither
# compiled nor tested again.
org.gradle.caching=true
