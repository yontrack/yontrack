target
.idea
*.iml
.run
.project
.classpath
.settings
work*/
!workflows
.DS_Store
build
.gradle
/*.txt
*.pid
/*-tests.xml
/version.properties
/release.sh
/release
/extensions
/LOG_FILE_IS_UNDEFINED*
/publication
/migration
out/
/delivery

# Build artifacts
codecov.sh

# Extension web files
ontrack-extension-*/package*.json
ontrack-extension-*/gulpfile.js
ontrack-extension-*/node_modules

# CI & Ontrack CLI
/.ontrack-cli*

# Local Casc configuration
/casc


# `build` above is unanchored, so it also matches the net.nemerosa.ontrack.build *package*
# directory under buildSrc, silently dropping any new source file added there.
!buildSrc/src/main/kotlin/net/nemerosa/ontrack/build/
!buildSrc/src/test/kotlin/net/nemerosa/ontrack/build/

# Local development stack state (scripts/dev-stack.sh)
.yontrack-dev/

# Kotlin compiler daemon session files
.kotlin/

# Local MCP server configuration, written by the IDE and pointing at a port on this machine
.mcp.json
