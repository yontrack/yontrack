mutation SetProjectLabels(
    $projectId: Int!,
    $labelIds: [Int!]!,
) {
    setProjectLabels(input: {
        projectId: $projectId,
        labelIds: $labelIds,
    }) {
        ...PayloadUserErrors
    }
}
