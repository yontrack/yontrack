query ValidationRunFindings(
    $id: Int!,
) {
    validationRuns(id: $id) {
        findings {
            severity
            rawSeverity
            installedVersion
            fixedVersion
            finding {
                ...FindingFragment
            }
        }
    }
}
